name: o2vie-chatel.ch deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    if:  ${{ startsWith(github.event.release.tag_name, 'o2vie-') }}
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./o2vie-chatel.ch

    steps:
    - uses: actions/checkout@v2
      
    - uses: actions/setup-node@v2
      with:
        node-version: 15
    
    - name: Install dependencies
      run: |
        npm install
        npm install -g gatsby-cli
        
    - name: Build
      run: |
        echo "DIRECTUS_O2VIE_TOKEN=${{secrets.O2VIE_CH_DIRECTUS_O2VIE_TOKEN}}" >> .env.production
        gatsby build

    - name: Get the version
      id: version_file
      # output "o2vie-v1_2_3"
      run: echo ::set-output name=VERSION::$(echo ${github.event.release.tag_name} | tr . _)

    - name: Create tar.gz
      run: |
        tar -czvf ${{ steps.version_file.outputs.VERSION }}.tar.gz public

    - name: Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: o2vie_*.tar.gz
      id: release

    - name: Trigger deploy
      run: |
        echo "Release to be published: ${{ fromJSON(steps.release.outputs.assets)[0].browser_download_url }}"
        curl -X POST -d "{\"browser_download_url\": \"${{ fromJSON(steps.release.outputs.assets)[0].browser_download_url }}\"} https://hodor.doxatech.ch/api/hook/o2vie

